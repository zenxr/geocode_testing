 .. _users:

Users
======================================================================

Default User model from Cookie-Cutter Django. Only exception is a `prefer_metric` field.

This model behaves identically to the default user model, 
but you’ll be able to customize it in the future if the need arises.

.. automodule:: geocache_api.users.models
   :members:
   :noindex:

